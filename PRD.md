# 跑步节奏增强音乐工具 PRD（草案）

---

## 1. 需求概述（Scope）

帮助跑步者在上传的歌曲中，附加符合节奏的鼓点，让用户可以一边听自己喜欢的歌、一边用清晰稳定的鼓点作为「节奏参考」，从而辅助控制步频（步频通常与 BPM 对应或为其倍数）。

**本产品特点：**
- 只面向「跑步」场景，文案、交互均以跑步为核心。
- 保留原曲，不改变旋律/音色，只额外叠加一条鼓点轨道。
- 全流程在浏览器本地完成（纯前端），无需安装软件、无需上传到服务器。

---

## 2. 用户与使用场景

### 2.1 用户画像

- 经常跑步 / 室内机跑的运动爱好者。
- 使用手机听歌跑步，但更偏好在电脑端先处理好歌曲，再同步至手机或跑步设备。
- 对节奏和步频比较敏感，希望通过节拍来保持稳定配速。

### 2.2 典型使用场景

1. 用户有一张喜欢的跑步歌单，但每首歌 BPM 不统一，跑步节奏容易乱。
   - 用户在电脑端打开本工具，逐首上传歌曲，添加统一 BPM 的鼓点轨道，导出后组成自己的「节奏增强跑步歌单」。

2. 用户只想轻微的节奏提示，不想破坏原曲氛围。
   - 在编辑时选择轻量鼓点模板（如小音量 Hihat），并控制鼓点音量，使其只作为背景节拍。

---

## 3. 产品目标与成功标准

### 3.1 产品目标

- 为跑步者提供一套极简的在线工具，让其快速将任意喜欢的歌曲，变成适合以某个节奏（BPM）跑步的版本。
- 提供「自动 BPM 检测 + 用户自定义 BPM」两种模式，满足不同用户需求。
- 支持可视化时间线编辑，让用户能直观查看和微调鼓点。

### 3.2 成功标准（初步）

- 从进入页面到完成一次歌曲处理并下载，全流程控制在 1–3 分钟。
- 对 3–6 分钟的常见歌曲，BPM 分析时间控制在 5–15 秒，导出 MP3 时间控制在 10–40 秒（视设备性能）。
- 用户无需阅读帮助文档，仅凭页面引导即可完成操作。

---

## 4. 核心功能（Objectives）

### 4.1 音乐上传

- 支持用户在网页中上传本地音乐文件。
- 支持的基础格式（受限于浏览器解码能力，具体以实现时确认为准）：
  - `mp3 / wav / m4a / aac / flac` 中的常见情况。
- 约束（可配置）：
  - 单文件大小上限（建议 20–50 MB，MVP 可先定 20 MB）。
  - 单首歌曲时长上限（建议 10 分钟）。
- 错误处理：
  - 文件格式不支持：给出清晰提示。
  - 文件损坏/无法解码：提示用户更换文件。

### 4.2 BPM 分析（自动检测）

- 在前端使用 Web Audio API 对上传的歌曲进行 BPM 分析。
- 分析结果展示：
  - 显示检测到的 BPM 数值（可保留小数，如 159.7）。
  - 显示分析可信度（高 / 中 / 低）。
- 结果微调能力：
  - 用户可在输入框中手动修改 BPM（例如把 159.7 改成 160）。
  - 提供倍频/半频调整按钮：
    - 「×2」：将 80 调整为 160 等。
    - 「÷2」：将 180 调整为 90 等。

### 4.3 BPM 控制逻辑（自动 vs 自定义）

提供两种模式，用户可在前端显式选择：

1. **基于自动检测的模式**  
   - 默认模式。  
   - 页面逻辑：  
     - 使用自动检测 BPM 作为「默认目标 BPM」。  
     - 用户可以在此基础上微调（如 158 → 160）。  

2. **完全自定义 BPM 模式**  
   - 用户可切换到「自定义 BPM」选项，不再受自动检测结果限制。  
   - 用户可以输入任意 BPM（在合理范围内，如 60–220）。  
   - 多个歌曲之间可以统一 BPM（如所有歌都设为 170）。  

交互上可通过一个选择器实现：  
- 选项 A：「根据歌曲节奏（自动检测基础上微调）」  
- 选项 B：「我只想用自己的目标 BPM」  

### 4.4 鼓点轨道与预设（Pattern）

- 提供多个预设鼓点 Pattern，并支持后续扩展：
  - 基础四拍（每一拍一个 Kick）。
  - 跑步双拍（每一拍两下，如 1-&-2-&-3-&-4-&）。
  - 轻量节拍（小音量 Hihat、Clap 为主）。
- 提供若干预设组合包（鼓点风格）供一键选择，不暴露太多底层音色选择，以保持简单。
- 控制参数：
  - 目标 BPM（来自前文两种逻辑中的当前值）。
  - 鼓点整体音量（0–100%）。
  - 淡入/淡出开关（可选，用于避免一开始或结尾鼓点过突兀）。

### 4.5 可视化时间线编辑

在保持整体简洁的前提下，加入基础可视化时间线编辑功能：

- 时间线基础：
  - 横轴为时间（以秒为单位，或以小节/拍为单位）。
  - 显示已加入的鼓点事件（如小圆点/短线，按轨道颜色区分）。
  - 支持缩放时间轴（最简可以支持两档：整体/细节）。

- 基本编辑能力（MVP 建议）：
  - 鼠标点击时间线可添加单个鼓点（在最近的节拍网格点对齐）。
  - 拖动现有鼓点事件进行微调（稍微偏离网格时自动吸附回最近拍点，可配置吸附强度）。
  - 右键或点击某个图标删除鼓点事件。
  - 在鼓点 Pattern 生成后，用户可以在可视化时间线上进行细节修改（增删改）。

- 视觉简化建议：
  - 不做多轨 DAW 级别复杂设计，仅有 1 条「鼓点轨」+ 背景波形（可选）。
  - 鼓点可统一用一种清晰颜色表示，或为不同音色使用不同图标/颜色，但保持简单易懂。

### 4.6 预览与播放控制

- 预览内容：
  - 实时播放「原曲 + 当前鼓点配置（包括时间线编辑结果）」的混音效果。
- 控件：
  - 播放 / 暂停。
  - 播放进度条，支持拖动。
  - 总体播放音量控制。
- 要求：
  - 切换鼓点预设、修改 BPM/音量、在时间线上增删鼓点后，预览效果应尽可能实时更新。

### 4.7 导出与下载（MP3）

- 导出格式：
  - 固定为 `mp3`。
  - 码率：默认 192 kbps，可在设置中提供简单选项（如 128 / 192 / 320）。
- 行为：
  - 用户点击「生成并下载」按钮。
  - 前端使用离线渲染混音结果，并通过纯前端 MP3 编码库（如 `lamejs` / `ffmpeg.wasm`）生成 MP3。
  - 编码完成后，通过浏览器自动下载，文件名建议格式：  
    `原文件名_bpm{目标BPM}_runmix.mp3`
- 进度与状态：
  - 导出过程中展示进度状态（如进度条或「处理中…」提示）。
  - 导出失败时，给出重试选项和简单错误文案（内存不足、浏览器不支持等）。

### 4.8 示例歌曲 / DEMO

- 提供至少 1 首内置示例歌曲或示例鼓点项目，用于首次访问时的引导体验：
  - 不要求完整商业曲目，可用免版权示例音频。
  - 用户可一键加载 DEMO，直接体验「检测 → 鼓点 → 时间线 → 导出」流程。
- DEMO 使用场景：
  - 未上传文件时，时间线可显示 DEMO 的波形与鼓点示意。
  - 可在顶部显眼位置放一个「试用示例歌曲」按钮。

---

## 5. 交互流程（Flow）

1. 打开网页  
   - 看到大区域的上传框（Drag & Drop + 点击选择）和一个「试用示例歌曲」按钮。  

2. 上传音乐或使用 DEMO  
   - 上传本地文件：前端解码后显示歌曲信息（文件名、时长等）。  
   - 使用 DEMO：直接加载内置音频及默认鼓点设置。  

3. 自动分析 BPM  
   - 自动开始 BPM 检测，显示「正在分析节奏…」的加载状态。  
   - 完成后展示检测结果及可信度。  

4. 选择 BPM 控制模式  
   - 默认选择「基于歌曲节奏」模式，自动填入检测到的 BPM（可修改）。  
   - 用户可切换到「自定义 BPM」模式，设置任意目标 BPM。  

5. 选择鼓点预设并调整参数  
   - 选择鼓点风格（如基础四拍、跑步双拍等）。  
   - 调整鼓点整体音量。  
   - 选择是否开启淡入/淡出。  

6. 时间线查看与微调  
   - 自动按当前 BPM 和鼓点预设生成基础鼓点轨，并在时间线上展示。  
   - 用户可在时间线上增删或拖动鼓点事件进行微调。  

7. 预览试听  
   - 点击播放，实时听到「原曲 + 鼓点」混音。  
   - 若不满意，可回到参数和时间线调整。  

8. 导出并下载 MP3  
   - 点击「生成并下载」，等待处理完成。  
   - 浏览器自动下载生成好的 MP3，用户可保存并导入至自己的音乐播放器或跑步 App。  

---

## 6. 技术方案（Tech）

### 6.1 整体架构

- 前端单页应用（SPA），技术栈可选：
  - React / Vue / Svelte 等任一主流框架，或极简原生实现。
- 纯前端实现：
  - 使用 Web Audio API 完成解码、分析、混音。
  - 使用 JS/WASM 库完成 MP3 编码。
  - 所有音频数据仅在用户本地浏览器内存中处理，不上传至服务器。

### 6.2 核心技术点

1. **音频解码**  
   - 利用 `AudioContext.decodeAudioData` 将上传的音频文件解码为 `AudioBuffer`。  

2. **BPM 检测**  
   - 方案 A：选型稳定维护的 BPM 检测库（需调研 2026 年可用方案）。  
   - 方案 B：自研简化版算法，对单声道数据做能量变化/自相关分析，提取主周期并换算 BPM。  
   - 在前端加一层逻辑处理倍频/半频、结果过滤和平滑。  

3. **鼓点生成与时间线**  
   - 鼓点音色使用内置短音频（kick/snare/hihat 等）。  
   - 根据目标 BPM 计算每一拍时间，生成一组「节拍网格」。  
   - 预设 Pattern 在网格上生成一系列事件。  
   - 时间线显示这些事件，并支持增删拖动，最终导出为事件列表。  

4. **混音与导出**  
   - 使用 `OfflineAudioContext` 离线渲染：  
     - 加载原曲 AudioBuffer。  
     - 按事件列表在合适时间点触发鼓点 AudioBufferSourceNode。  
     - 使用 `GainNode` 控制鼓点音量及淡入/淡出。  
   - 渲染完成得到混合后的 AudioBuffer。  
   - 使用 `lamejs` 或 `ffmpeg.wasm` 将 PCM 数据编码为 MP3，并提供下载。  
   - 考虑将编码过程放入 Web Worker，避免阻塞 UI。  

### 6.3 运行环境与兼容性

- 仅支持桌面浏览器（优先 Chrome / Edge / Safari，版本要求待定）。  
- 首次加载时检测关键 API（Web Audio、WebAssembly 等），若不支持则给出提示。  

---

## 7. 非功能性需求

- **性能**：  
  - 常规 3–6 分钟歌曲，BPM 检测与导出时间保持在可接受范围内（总处理时间控制在 1–2 分钟内为佳）。  
- **隐私与安全**：  
  - 不上传原始音频文件到服务器，不做后端存储。  
  - 页面中明确说明「所有处理在本地浏览器完成」。  
- **可用性**：  
  - 步骤清晰，每一步仅展示当前关键操作避免信息过载。  
- **UI 风格**：  
  - 极简、清晰，颜色搭配合理但不过于花哨。  
  - 优先亮色主题，适当使用跑步/运动相关的图标或插画作点缀。  

---

## 8. MVP 范围（首版上线必须具备）

1. 上传本地音乐（桌面端浏览器）。  
2. 基础 BPM 自动检测，并展示结果。  
3. 支持「基于检测 BPM 微调」和「完全自定义 BPM」两种模式。  
4. 至少 2–3 种鼓点预设 Pattern。  
5. 简单可视化时间线（显示鼓点事件 + 增删拖动能力）。  
6. 预览播放（原曲 + 鼓点）。  
7. 导出并下载 MP3。  
8. 示例歌曲/DEMO 一键体验流程。  

非 MVP 但可优先排在后续版本的包括：更多高级编辑功能、更丰富音色和 Pattern、移动端适配、用户配置持久化等。
